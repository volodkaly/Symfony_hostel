<IfModule mod_rewrite.c>
    RewriteEngine On

    # Якщо файл або директорія існує, використовуємо їх
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Інакше відправляємо на index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>

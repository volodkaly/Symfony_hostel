{% extends 'base.html.twig' %}

{% block title %}
  Review index
{% endblock %}

{% block body %}
  {% block header %}
    {{ parent() }}
  {% endblock %}

  <div class="example-wrapper">
    <a href="{{ path('app_room') }}"><h4>All rooms</h4></a>
    <h4>My reviews</h4>
    <h6>On this page your reviews are only available for reading.</h6>
    <h6>To leave a new review choose one of your bookings.</h6>

    <table class="table">
      <thead>
        <tr>
          <th>Id</th>
          <th>Title</th>
          <th>Mark</th>
          <th>Description</th>
          <th>Booking Id</th>
          <th>Customer</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {% for review in reviews %}
          <tr>
            <td>{{ review.id }}</td>
            <td>{{ review.title }}</td>
            <td>{{ review.mark }}</td>
            <td>{{ review.description }}</td>
            <td>{{ review.booking.id }}</td>
            <td>{{ review.booking.customer.name }}</td>
            <td>
              <a href="{{ path('app_review_show', { id: review.id }) }}" class="btn btn-primary">show</a>
              <a href="{{ path('app_review_edit', { id: review.id }) }}" class="btn btn-primary">edit</a>
            </td>
          </tr>
        {% else %}
          <tr>
            <td colspan="5">no records found</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
{% endblock %}

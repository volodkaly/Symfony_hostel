{% extends 'base.html.twig' %}

{% block title %}
  Message index
{% endblock %}

{% block body %}
  {% block header %}
    {{ parent() }}
  {% endblock %}
  <h1>Message index</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Id</th>
        <th>Content</th>
        <th>CreatedAt</th>
        <th>Sender</th>
        <th>Recipient</th>

        <th>actions</th>
      </tr>
    </thead>
    <tbody>
      {% for message in messages %}
        <tr>
          <td>{{ message.id }}</td>
          <td>{{ message.content }}</td>
          <td>{{ message.createdAt ? message.createdAt|date('Y-m-d H:i:s') : '' }}</td>
          <td>{{ message.sender.name ~ ' (id:' ~ message.sender.id ~ ') ' ~ ' (email:' ~ message.sender.email ~ ') ' }}</td>
          <td>{{ message.recipient.name ~ ' (id:' ~ message.recipient.id ~ ') ' ~ ' (email:' ~ message.recipient.email ~ ') ' }}</td>
          <td>
            <a href="{{ path('app_message_show', { id: message.id }) }}">show</a>
            <a href="{{ path('app_message_edit', { id: message.id }) }}">edit</a>
          </td>
        </tr>
      {% else %}
        <tr>
          <td colspan="4">no records found</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

  <a href="{{ path('app_message_new') }}">Create new</a>
{% endblock %}
